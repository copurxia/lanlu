[package]
  cjc-version = "1.0.4"
  name = "webp_ffi"
  description = "WebP image decoder (libwebp) FFI bindings"
  version = "0.1.0"
  output-type = "static"
  # Animated WebP decoding lives in libwebpdemux (WebPAnimDecoder*).
  link-option = "-lwebpdemux -lwebp"

[dependencies]
