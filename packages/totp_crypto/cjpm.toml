[package]
  cjc-version = "1.1.0"
  name = "totp_crypto"
  version = "0.1.0"
  output-type = "static"
  description = "Envelope v1 AEAD encryption (StdxFallback, ignite-compatible format) for TOTP secret storage"

[dependencies]