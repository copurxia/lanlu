[package]
name = "lanlu-webview-native"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
name = "webview_native"
crate-type = ["staticlib"]

[dependencies]
blitz-dom = "=0.3.0-beta.1"
blitz-html = "=0.3.0-beta.1"
blitz-paint = "=0.3.0-beta.1"
blitz-traits = "=0.3.0-beta.1"
anyrender = "=0.11.0"
anyrender_vello_cpu = "=0.14.0"
peniko = "0.6"

# 仅诊断用：与 blitz 同版本 parley，from_html 前 fontique 预初始化二分崩溃点。
parley = "=0.10.0"
